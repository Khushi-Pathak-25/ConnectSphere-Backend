spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/paymentdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Khushi@8251082191
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  boot:
    admin:
      client:
        url: http://localhost:9090
        username: admin
        password: admin123

server:
  port: 8089

razorpay:
  key:
    id: rzp_test_SeTlPSHAzFfarU
    secret: FvZOBWcd8eFofOsdHeGBMloq

auth:
  service:
    url: http://localhost:8081

post:
  service:
    url: http://localhost:8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,loggers,env,logfile
  endpoint:
    health:
      show-details: always
    logfile:
      enabled: true
      external-file: logs/payment-service.log

info:
  app:
    name: payment-service
    version: 1.0.0
